<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="ListView" x:Key="ProductsListView">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Margin="10"
                                        Background="{StaticResource BackgroundWhite}"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        CornerRadius="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="12"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="12"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource ButtonColor}" CornerRadius="10,0,0,10"
                                                x:Name="SideBorder1"/>
                                        <Image Source="{Binding CorrectPicturePath, UpdateSourceTrigger=PropertyChanged}"
                                               Height="100"
                                               Width="100"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Grid.Column="1"
                                               Margin="5"/>
                                        <StackPanel Grid.Column="2"
                                                    Margin="5">
                                            <StackPanel>
                                                <TextBlock Text="НОВИНКА:"
                                                           FontSize="14"
                                                           x:Name="TextBlock3"
                                                           Style="{StaticResource TextBlock3}"
                                                           FontWeight="Bold"
                                                           Visibility="Collapsed"/>
                                                <TextBlock Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Bold"
                                                       FontSize="16"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                                       TextWrapping="Wrap"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Medium"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Margin="5">
                                            <TextBlock Text="{Binding CorrectCost, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat={}{0:c}}"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"/>
                                            <TextBlock x:Name="TextBlock1"
                                                       Text="{Binding Cost, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat={}{0:c}}"
                                                       TextDecorations="Strikethrough"
                                                       Visibility="Collapsed"/>
                                            <TextBlock x:Name="TextBlock2"
                                                       Text="{Binding CorrectDiscount,StringFormat=Скидка: {0}%}"
                                                       Visibility="Collapsed"/>
                                        </StackPanel>
                                        <Border Grid.Column="4" Background="{StaticResource ButtonColor}" CornerRadius="0,10,10,0"
                                                x:Name="SideBorder2"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsDiscount}" Value="true">
                                        <Setter TargetName="TextBlock1" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="TextBlock2" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsNew}" Value="true">
                                        <Setter TargetName="TextBlock3" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListView" x:Key="ServicesListView">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Margin="10"
                                        Background="{StaticResource BackgroundWhite}"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        CornerRadius="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="12"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="12"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource ButtonColor}" CornerRadius="10,0,0,10"
                                                x:Name="SideBorder1"/>
                                        <Image Source="{Binding CorrectPicturePath, UpdateSourceTrigger=PropertyChanged}"
                                               Height="100"
                                               Width="100"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Grid.Column="1"
                                               Margin="5"/>
                                        <StackPanel Grid.Column="2"
                                                    Margin="5">
                                            <StackPanel>
                                                <TextBlock Text="НОВИНКА:"
                                                           FontSize="14"
                                                           x:Name="TextBlock3"
                                                           Style="{StaticResource TextBlock3}"
                                                           FontWeight="Bold"
                                                           Visibility="Collapsed"/>
                                                <TextBlock Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Bold"
                                                       FontSize="16"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                                       TextWrapping="Wrap"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Medium"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Margin="5">
                                            <TextBlock Text="{Binding CostPerHour, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat={}{0:c}}"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"
                                                       x:Name="TextBlock1"/>
                                            <TextBlock Text="Договорная"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"
                                                       x:Name="TextBlock2"
                                                       Visibility="Collapsed"/>
                                        </StackPanel>
                                        <Border Grid.Column="4" Background="{StaticResource ButtonColor}" CornerRadius="0,10,10,0"
                                                x:Name="SideBorder2"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsNegotiable}" Value="true">
                                        <Setter TargetName="TextBlock1" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="TextBlock2" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsNew}" Value="true">
                                        <Setter TargetName="TextBlock3" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListView" x:Key="ProductOrdersListView">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Margin="10"
                                        Background="{StaticResource BackgroundWhite}"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        CornerRadius="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="12"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="12"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource ButtonColor}" CornerRadius="10,0,0,10"
                                                x:Name="SideBorder1"/>
                                        <Image Source="{Binding Product.CorrectPicturePath, UpdateSourceTrigger=PropertyChanged}"
                                               Height="100"
                                               Width="100"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Grid.Column="1"
                                               Margin="5"/>
                                        <StackPanel Grid.Column="2"
                                                    Margin="5">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Product.Title, UpdateSourceTrigger=PropertyChanged, StringFormat=Товар: {0}}"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Bold"
                                                       FontSize="16"/>
                                                <TextBlock Text="{Binding DateOfAdd, UpdateSourceTrigger=PropertyChanged,ConverterCulture=ru-RU, StringFormat=Дата заказа: {0:d}}"
                                                       Style="{StaticResource TextBlock2}"/>
                                                <TextBlock Text="{Binding DateOfComplete, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat=Дата завершения: {0:d}}"
                                                           Style="{StaticResource TextBlock2}"
                                                           x:Name="TextBlock1"
                                                           Visibility="Collapsed"/>
                                                <TextBlock Text="{Binding Employee.FullName, UpdateSourceTrigger=PropertyChanged, StringFormat=Сотрудник: {0}}"
                                                           Style="{StaticResource TextBlock2}"
                                                           x:Name="TextBlock2"
                                                           Visibility="Collapsed"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Margin="5">
                                            <TextBlock Text="{Binding PaymentAmount, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat={}{0:c}}"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"/>
                                        </StackPanel>
                                        <Border Grid.Column="4" Background="{StaticResource ButtonColor}" CornerRadius="0,10,10,0"
                                                x:Name="SideBorder2"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsCompleted}" Value="true">
                                        <Setter TargetName="TextBlock1" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="TextBlock2" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ListView" x:Key="ServiceOrdersListView">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Margin="10"
                                        Background="{StaticResource BackgroundWhite}"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        CornerRadius="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="12"/>
                                            <ColumnDefinition Width="120"/>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="12"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Background="{StaticResource ButtonColor}" CornerRadius="10,0,0,10"
                                                x:Name="SideBorder1"/>
                                        <Image Source="{Binding Service.CorrectPicturePath, UpdateSourceTrigger=PropertyChanged}"
                                               Height="100"
                                               Width="100"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Grid.Column="1"
                                               Margin="5"/>
                                        <StackPanel Grid.Column="2"
                                                    Margin="5">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Service.Title, UpdateSourceTrigger=PropertyChanged}"
                                                       Style="{StaticResource TextBlock2}"
                                                       FontWeight="Bold"
                                                       FontSize="16"/>
                                                <TextBlock Text="{Binding DateOfAdd, UpdateSourceTrigger=PropertyChanged,ConverterCulture=ru-RU, StringFormat=Дата заказа: {0:d}}"
                                                       Style="{StaticResource TextBlock2}"/>
                                                <TextBlock Text="{Binding DateOfComplete, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat=Дата завершения: {0:d}}"
                                                           Style="{StaticResource TextBlock2}"
                                                           x:Name="TextBlock3"
                                                           Visibility="Collapsed"/>
                                                <TextBlock Text="{Binding Employee.FullName, UpdateSourceTrigger=PropertyChanged, StringFormat=Сотрудник: {0}}"
                                                           Style="{StaticResource TextBlock2}"
                                                           x:Name="TextBlock4"
                                                           Visibility="Collapsed"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Margin="5">
                                            <TextBlock Text="{Binding Service.CostPerHour, UpdateSourceTrigger=PropertyChanged, ConverterCulture=ru-RU, StringFormat={}{0:c}}"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"
                                                       x:Name="TextBlock1"/>
                                            <TextBlock Text="Договорная"
                                                       FontSize="14"
                                                       Style="{StaticResource TextBlock2}"
                                                       x:Name="TextBlock2"
                                                       Visibility="Collapsed"/>
                                        </StackPanel>
                                        <Border Grid.Column="4" Background="{StaticResource ButtonColor}" CornerRadius="0,10,10,0"
                                                x:Name="SideBorder2"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorMouseOver}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="SideBorder1" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                        <Setter TargetName="SideBorder2" Property="Background" Value="{StaticResource ButtonColorPressed}"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding Service.IsNegotiable}" Value="true">
                                        <Setter TargetName="TextBlock1" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="TextBlock2" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsCompleted}" Value="true">
                                        <Setter TargetName="TextBlock3" Property="Visibility" Value="Visible"/>
                                        <Setter TargetName="TextBlock4" Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>